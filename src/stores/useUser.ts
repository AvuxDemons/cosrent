// import { create } from "zustand";

// interface UserPersonalInfo {
//     currentSession: any;
//     setSession: (data: any) => void;
// }

// const useUserPersonalInfo = create<UserPersonalInfo>((set) => ({
//     : null,
//     setSession: (data) => set({ currentSession: data }),
// }));

// export default useSession;
